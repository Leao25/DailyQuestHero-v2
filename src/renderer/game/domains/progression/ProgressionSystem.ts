import type { GameScene } from '../../scenes/GameScene'

// Thin wrapper — stats vivem no Hero, este sistema pode escalar para bônus, missões, etc.
export class ProgressionSystem {
  constructor(_scene: GameScene) {}
}
